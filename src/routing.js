"use strict";

/**
 * Auth class
 *
 * @class Auth
 */
export default class Auth {

    /**
     * Creates an instance of Auth.
     *
     * @param {Object} options
     *
     * @memberof Auth
     */
    constructor(options) {
        console.log("Create a new Auth instance");
    }

    //TODO implement auth adapters
}