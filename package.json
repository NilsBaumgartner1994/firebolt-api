{
  "name": "firebolt-api",
  "version": "0.0.19",
  "description": "Firebolt-API",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node ./build/development.js",
    "build": "rimraf ./build && tsc",
    "dev": "nodemon",
    "prepareUpload": "npm run build && cp package.json ./build",
    "upload": "npm run prepareUpload && cd ./build && npm version patch && npm publish && cd .. && npm version patch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NilsBaumgartner1994/firebolt-api.git"
  },
  "keywords": [
    "firebolt",
    "api"
  ],
  "author": "Nils Baumgartner",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/NilsBaumgartner1994/firebolt-api/issues"
  },
  "homepage": "https://github.com/NilsBaumgartner1994/firebolt-api#readme",
  "devDependencies": {
    "@types/node": "^16.0.0",
    "nodemon": "^2.0.9",
    "rimraf": "^3.0.2",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.5"
  }
}
